///
/// HybridASWebAuthenticationSessionSpecCxx.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2024 Marc Rousavy @ Margelo
///

import Foundation
import NitroModules

/**
 * A class implementation that bridges HybridASWebAuthenticationSessionSpec over to C++.
 * In C++, we cannot use Swift protocols - so we need to wrap it in a class to make it strongly defined.
 *
 * Also, some Swift types need to be bridged with special handling:
 * - Enums need to be wrapped in Structs, otherwise they cannot be accessed bi-directionally (Swift bug: https://github.com/swiftlang/swift/issues/75330)
 * - Other HybridObjects need to be wrapped/unwrapped from the Swift TCxx wrapper
 * - Throwing methods need to be wrapped with a Result<T, Error> type, as exceptions cannot be propagated to C++
 */
public class HybridASWebAuthenticationSessionSpecCxx {
  /**
   * The Swift <> C++ bridge's namespace (`margelo::nitro::inappbrowser::bridge::swift`)
   * from `NitroInAppBrowser-Swift-Cxx-Bridge.hpp`.
   * This contains specialized C++ templates, and C++ helper functions that can be accessed from Swift.
   */
  public typealias bridge = margelo.nitro.inappbrowser.bridge.swift

  /**
   * Holds an instance of the `HybridASWebAuthenticationSessionSpec` Swift protocol.
   */
  private var implementation: HybridASWebAuthenticationSessionSpec

  /**
   * Get the actual `HybridASWebAuthenticationSessionSpec` instance this class wraps.
   */
  @inline(__always)
  public func getHybridASWebAuthenticationSessionSpec() -> HybridASWebAuthenticationSessionSpec {
    return implementation
  }

  /**
   * Create a new `HybridASWebAuthenticationSessionSpecCxx` that wraps the given `HybridASWebAuthenticationSessionSpec`.
   * All properties and methods bridge to C++ types.
   */
  public init(_ implementation: HybridASWebAuthenticationSessionSpec) {
    self.implementation = implementation
    /* no base class */
  }

  /**
   * Contains a (weak) reference to the C++ HybridObject to cache it.
   */
  public var hybridContext: margelo.nitro.HybridContext {
    @inline(__always)
    get {
      return self.implementation.hybridContext
    }
    @inline(__always)
    set {
      self.implementation.hybridContext = newValue
    }
  }

  /**
   * Get the memory size of the Swift class (plus size of any other allocations)
   * so the JS VM can properly track it and garbage-collect the JS object if needed.
   */
  @inline(__always)
  public var memorySize: Int {
    return self.implementation.memorySize
  }

  // Properties
  public var canStart: Bool {
    @inline(__always)
    get {
      return self.implementation.canStart
    }
    @inline(__always)
    set {
      self.implementation.canStart = newValue
    }
  }
  
  public var prefersEphemeralWebBrowserSession: Bool {
    @inline(__always)
    get {
      return self.implementation.prefersEphemeralWebBrowserSession
    }
    @inline(__always)
    set {
      self.implementation.prefersEphemeralWebBrowserSession = newValue
    }
  }

  // Methods
  @inline(__always)
  public func start(params: ASWebAuthenticationSessionStartParams) -> Void {
    do {
      try self.implementation.start(params: params)
      return 
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
  
  @inline(__always)
  public func cancel() -> Void {
    do {
      try self.implementation.cancel()
      return 
    } catch {
      let message = "\(error.localizedDescription)"
      fatalError("Swift errors can currently not be propagated to C++! See https://github.com/swiftlang/swift/issues/75290 (Error: \(message))")
    }
  }
}
